import pandas as pd
import json, csv
from openpyxl import load_workbook


#with open('inv.csv', 'rt') as csvdata:
#    next(csvdata, None) # skip the headers
#    reader = csv.DictReader(csvdata,fieldnames=['Column', 'Bottle from top (Number)','Year, Vintage, or NV', 'Name', 'Type'])
#    json.dump([row for row in reader], open('output.json', 'w+'))
